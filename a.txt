Bon la partie adhérent de la reservation (Réserver un livre) marche:
Maintenant passons a la partie bibliothecaire pour la reservation:
ce que fait le bibliothecaire est de valider la reservation si la reservation est valider ca devient un pret.
Donc dans la partie affichage de la reservation du bibliothecaire nous devons avoir la liste des demandes de reservation. et un boutton pour valider.

dans la partie adhérent nous allons ajouter qualque regle nous avons ses regle dans la tables:
CREATE TABLE
    Regle (
        id_regle SERIAL,
        nb_jour_duree_pret_max INT NOT NULL,
        nb_livre_preter_max INT NOT NULL,
        nb_prolengement_pret_max INT NOT NULL,
        nb_jour_prolongement_max INT NOT NULL,
        PRIMARY KEY (id_regle)
    );
dans cette table on se concentre plutot nb_livre_preter_max.
. il y a aussi le fait que on ne peut faire qu'un reservation.
Donc si on a deja une demande de reservation actif on ne peut pas reserver. On peut considerer que la demande n'est plus actif quand ca devinent un pret
